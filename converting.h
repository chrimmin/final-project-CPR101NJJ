#ifndef _CONVERTING_H_		// checks if a macro is not defined
#define _CONVERTING_H_		// defines the macro 
#include <stdio.h>		// includes the contents of stdio.h into the header file
#include <string.h>		// includes the contents of string.h into the header file
#include <stdlib.h>		// includes the contents of stdlib.h into the header file
void converting(void);
#endif		// ends the ifndef 